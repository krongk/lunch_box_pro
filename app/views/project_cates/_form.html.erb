<%= form_for(@project_cate) do |f| %>
  <% if @project_cate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_cate.errors.count, "error") %> prohibited this project_cate from being saved:</h2>

      <ul>
      <% @project_cate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
