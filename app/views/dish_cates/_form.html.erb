<%= form_for(@dish_cate) do |f| %>
  <% if @dish_cate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish_cate.errors.count, "error") %> prohibited this dish_cate from being saved:</h2>

      <ul>
      <% @dish_cate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
