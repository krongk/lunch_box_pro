<%= form_for(@news_cate) do |f| %>
  <% if @news_cate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@news_cate.errors.count, "error") %> prohibited this news_cate from being saved:</h2>

      <ul>
      <% @news_cate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
