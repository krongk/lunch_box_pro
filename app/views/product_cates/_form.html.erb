<%= form_for(@product_cate) do |f| %>
  <% if @product_cate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_cate.errors.count, "error") %> prohibited this product_cate from being saved:</h2>

      <ul>
      <% @product_cate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
